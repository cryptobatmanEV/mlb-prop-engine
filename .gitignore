.env
__pycache__/
*.pyc

# Generated daily outputs (committed by workflow after each run)
# Excluded from manual dev work but tracked so CI can read yesterday's CSV
data/predictions/

# Training dataset — large, regenerated by features/build_dataset.py
data/processed/training_dataset.parquet

# Batter Model (Hits/TB/Ks) training dataset — large, regenerated by
# features/build_batter_props_dataset.py
data/processed/batter_props_training_dataset.parquet

# Redundant model file (identical to hr_model.pkl after swap)
models/saved/hr_model_v2.pkl

# Web app build artifacts
.DS_Store
web/.env.local
web/node_modules/
web/.next/
web/tsconfig.tsbuildinfo
